import React from 'react';
import ReactDOM from 'react-dom';
import App from './0315/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('potato')
);
